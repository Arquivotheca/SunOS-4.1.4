/* There should be nothing in this file.
 * It is created dynamically in the Makefile.
 * It is only in SCCS in case "make" erroneously tries to do an "sccs get"
 * on it (I've seen this happen).
 */
